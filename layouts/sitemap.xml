{{ printf "<?xml version=\"1.0\" encoding=\"utf-8\" standalone=\"yes\" ?>" | safeHTML }}
<urlset xmlns="http://www.sitemaps.org/schemas/sitemap/0.9"
  xmlns:xhtml="http://www.w3.org/1999/xhtml">
  {{ $seen := dict }}
  {{ range .Site.Pages }}
  {{ if not (index $seen .Permalink) }}
  {{ $isPost := findRE "/posts/" .RelPermalink }}
  {{ $priority := cond (gt (len $isPost) 0) "0.8" "0.5" }}
  <url>
    <loc>{{ .Permalink }}</loc>{{ if not .Lastmod.IsZero }}
    <lastmod>{{ safeHTML ( .Lastmod.Format "2006-01-02T15:04:05-07:00" ) }}</lastmod>{{ end }}{{ with .Sitemap.ChangeFreq }}
    <changefreq>{{ . }}</changefreq>{{ end }}
    <priority>{{ $priority }}</priority>{{ if .IsTranslated }}{{ range .AllTranslations }}
    <xhtml:link
      rel="alternate"
      hreflang="{{ .Language.Lang }}"
      href="{{ .Permalink }}"
    />{{ end }}
    <xhtml:link
      rel="alternate"
      hreflang="{{ .Language.Lang }}"
      href="{{ .Permalink }}"
    />{{ end }}
  </url>
  {{ $seen = merge $seen (dict .Permalink true) }}
  {{ end }}
  {{ end }}
</urlset>
